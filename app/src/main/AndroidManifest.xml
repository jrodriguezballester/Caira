<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.caira15">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".CairaAplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Caira15"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".ui.principal.BodyappActivity"
            android:exported="false"
            android:label="@string/title_activity_bodyapp"
            android:theme="@style/Theme.Caira15.NoActionBar" />
        <activity
            android:name=".ui.Register.RegisterActivity"
            android:exported="false" />
        <activity
            android:name=".ui.dashboard.DashboardActivity"
            android:exported="false" />
        <activity
            android:name=".ui.login.InstruccionesActivity"
            android:exported="false"
            android:label="@string/title_activity_instrucciones"
            android:theme="@style/Theme.Caira15.NoActionBar" />
        <activity
            android:name=".ui.main.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>